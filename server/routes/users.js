const express = require('express');
const router = express.Router();
const User = require('../models/User');
const bcrypt = require('bcryptjs');

// 여기에 사용자 관련 라우트 구현
// 예: 회원가입, 로그인, 사용자 정보 조회 및 수정 등

module.exports = router;
